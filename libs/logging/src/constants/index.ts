export enum LOG_FORMATS {
  Plain = 'plain',
  JSON = 'json',
}

export enum LOGGER_LEVEL {
  Emergency = 'emergency',
  Alert = 'alert',
  Critical = 'critical',
  Error = 'error',
  Warning = 'warning',
  Notice = 'notice',
  Informational = 'informational',
  Debug = 'debug'
}
